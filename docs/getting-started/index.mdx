---
title: Getting Started
description: Learn the fastest path from first fetch to a full curl.md workflow.
---

# Getting Started

`curl.md` turns noisy webpages into markdown built for agents. Start with one fetch, then decide whether you want the browser URL, the CLI, or an agent integration.

## First Fetch

Use the hosted URL directly:

```sh
$ curl curl.md/example.com
```

Or use the CLI once it is installed:

```sh
$ md example.com
```

## Narrow The Result

When you only need part of a long page, add an objective and a few keywords:

```sh
$ md docs.github.com/en/webhooks/webhook-events-and-payloads \
  --objective "pull request webhook event payload and actions" \
  --keywords pull_request
```

This keeps the output focused on the part your agent actually needs.

## Pick A Workflow

:::card[Installation]{href=/docs/install icon=download}
Take the fastest setup path for installing the CLI and getting curl.md working locally.
:::

:::card[CLI]{href=/docs/guide/cli icon=terminal}
Use curl.md from the terminal, script fetches, and shape output with objectives and keywords.
:::

:::card[API & SDK]{href=/docs/guide/api icon=key}
Call `curl.md` from code and integrate markdown fetching into your app or tooling.
:::

:::card[Plugins]{href=/docs/guide/plugins icon=package}
Use Amp, Pi, and other native integrations when you want curl.md inside your agent or editor.
:::

## Read Next

:::card[Why curl.md]{href=/docs/why icon=lightbulb}
Understand why low-token markdown outputs make website context more useful for coding agents.
:::

:::card[Features]{href=/docs/guide/features icon=book}
Explore the main features, workflows, and output-shaping tools available across curl.md.
:::

:::card[Installation]{href=/docs/install icon=download}
Return to the installation guide when you're ready to set up the CLI locally.
:::
