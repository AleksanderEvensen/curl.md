---
title: Kitchen Sink
description: Preview how curl.md docs render common Markdown content.
---

# Kitchen Sink

Use this page to preview the common Markdown and notice styles supported by the docs renderer.

## Headings

### Level 3 Heading

#### Level 4 Heading

## Paragraphs & Links

Use **bold text**, _italic text_, and `inline code` for short callouts in prose.

Inline syntax highlighting works too: `pnpm dev{:sh}` or `curl.config.ts{:ts}`.

Link to [Installation](/docs/install) for setup instructions, or visit [GitHub](https://github.com/wevm/curl.md) for source code.

## Lists

- Unordered lists work for short collections.
- Nested items are supported.
  - This is a nested bullet.
  - This is another nested bullet.
- Keep bullets short when possible.

1. Ordered lists work well for step-by-step instructions.
2. They keep sequence obvious.
3. They also match the rest of the docs UI.

## Blockquotes

> Regular blockquotes still render as blockquotes.
>
> Use them for quotes that are not notices.

## Horizontal Rule

---

The separator above is a standard horizontal rule.

## Code Blocks

```sh
$ curl.md https://example.com
```

```json
{
  "runtime": "node",
  "watch": true
}
```

```ts title="example.ts"
import { create } from 'curl.md'
import { rules } from 'curl.md'

const md = create({ rules: [rules.githubRepo()] })
```

## Code Groups

:::codegroup

```sh [npm]
$ npm run dev
```

```sh [pnpm]
$ pnpm dev
```

```sh [bun]
$ bun run dev
```

:::

:::codegroup

```sh [npm]
$ npm install
```

```sh [pnpm]
$ pnpm install
```

```sh [bun]
$ bun install
```

:::

## Details

<details>
  <summary>Show extra guidance</summary>

<p>Native disclosure elements work in docs pages too.</p>

  <ul>
    <li>Use them for optional context.</li>
    <li>Keep the summary short and scannable.</li>
  </ul>
</details>

## Notices

:::note
Notices without a custom title default to the notice type.
:::

:::tip[Custom tip title]
Use titled notices when the label should be more specific than the default notice type.
:::

:::hint
Hints work well for shortcuts, alternatives, or small workflow improvements.
:::

:::important
Use important notices for behavior people should not miss.
:::

:::warning
Warnings should call out risky behavior or easy-to-make mistakes.
:::

:::danger
Danger notices are useful when a step can be destructive or hard to undo.
:::

:::tip
Tips still use the same shared notice component without relying on GitHub alert syntax.
:::

## Tables

| Runtime | Install Command                                      | Config File                         | Best For                    |
| ------- | ---------------------------------------------------- | ----------------------------------- | --------------------------- |
| Node.js | `pnpm add curl.md{:sh}`                              | `curl.config.ts{:text}`             | Existing TypeScript apps    |
| Bun     | `bun add curl.md{:sh}`                               | `bunfig.toml{:text}`                | Fast local tooling          |
| Deno    | `deno add jsr:@wevm/curl-md{:sh}`                    | `deno.json{:text}`                  | URL-first workflows         |
| CI      | `pnpm dlx curl.md https://example.com/rules.md{:sh}` | `.github/workflows/docs.yml{:text}` | One-off checks in pipelines |

## Steps

:::steps

### Install dependencies

Use your preferred package manager to install project dependencies.

### Start the dev server

```sh
$ pnpm dev
```

### Open the app

Visit [https://curl.local](https://curl.local) once the server is running.

:::

## Cards

:::card[Install curl.md]{href=/docs/install icon=rocket}
Start with the CLI for terminal and script usage.
:::

:::card[Amp plugin]{href=/docs/plugins/amp icon=book}
Enable docs fetch interception inside Amp.
:::

:::card[Authentication]{href=/docs/install#authentication icon=key}
Set up auth for higher rate limits and account features.
:::

## Package Links

<PackageLinks npm="curl.md" source="https://github.com/wevm/curl.md" />
