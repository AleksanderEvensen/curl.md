diff --git a/dist/esm/index.js b/dist/esm/index.js
index a3c085e..153f4b4 100644
--- a/dist/esm/index.js
+++ b/dist/esm/index.js
@@ -1,6 +1,6 @@
 import { DEFAULT_PROTOCOL_ALLOWLIST, buildDevStylesUrl, createControlledPromise, deepEqual, escapeHtml, functionalUpdate, isDangerousProtocol, isModuleNotFoundError, isPlainArray, isPlainObject, replaceEqualDeep } from "./utils.js";
 import { invariant } from "./invariant.js";
-import { cleanPath, exactPathTest, interpolatePath, joinPaths, removeTrailingSlash, resolvePath, trimPath, trimPathLeft, trimPathRight } from "./path.js";
+import { cleanPath, exactPathTest, interpolatePath, joinPaths, removeTrailingSlash, resolvePath, routePathToRegExpSource, trimPath, trimPathLeft, trimPathRight } from "./path.js";
 import { isNotFound, notFound } from "./not-found.js";
 import { defaultGetScrollRestorationKey, getElementScrollRestorationEntry, scrollRestorationCache, setupScrollRestoration, storageKey } from "./scroll-restoration.js";
 import { decode, encode } from "./qss.js";
@@ -14,6 +14,7 @@ import { TSR_DEFERRED_PROMISE, defer } from "./defer.js";
 import { preloadWarning } from "./link.js";
 import { createInlineCssStyleAsset, getAssetCrossOrigin, getStylesheetHref, isInlinableStylesheet, resolveManifestAssetLink } from "./manifest.js";
 import { isMatch } from "./Matches.js";
+import { getUnmaskOnReloadScript } from "./unmask-on-reload-script.js";
 import { BaseRootRoute, BaseRoute, BaseRouteApi } from "./route.js";
 import { createRouterConfig } from "./config.js";
 import { retainSearchParams, stripSearchParams } from "./searchMiddleware.js";
@@ -21,4 +22,4 @@ import { handleHashScroll } from "./hash-scroll.js";
 import { createSerializationAdapter, makeSerovalPlugin, makeSsrSerovalPlugin } from "./ssr/serializer/transformer.js";
 import { RawStream, createRawStreamDeserializePlugin, createRawStreamRPCPlugin } from "./ssr/serializer/RawStream.js";
 import { defaultSerovalPlugins } from "./ssr/serializer/seroval-plugins.js";
-export { BaseRootRoute, BaseRoute, BaseRouteApi, DEFAULT_PROTOCOL_ALLOWLIST, PathParamError, RawStream, RouterCore, SearchParamError, TSR_DEFERRED_PROMISE, buildDevStylesUrl, cleanPath, composeRewrites, createControlledPromise, createInlineCssStyleAsset, createNonReactiveMutableStore, createNonReactiveReadonlyStore, createRawStreamDeserializePlugin, createRawStreamRPCPlugin, createRouterConfig, createSerializationAdapter, decode, deepEqual, defaultGetScrollRestorationKey, defaultParseSearch, defaultSerializeError, defaultSerovalPlugins, defaultStringifySearch, defer, encode, escapeHtml, exactPathTest, executeRewriteInput, functionalUpdate, getAssetCrossOrigin, getElementScrollRestorationEntry, getInitialRouterState, getLocationChangeInfo, getMatchedRoutes, getStylesheetHref, handleHashScroll, interpolatePath, invariant, isDangerousProtocol, isInlinableStylesheet, isMatch, isModuleNotFoundError, isNotFound, isPlainArray, isPlainObject, isRedirect, isResolvedRedirect, joinPaths, lazyFn, makeSerovalPlugin, makeSsrSerovalPlugin, notFound, parseRedirect, parseSearchWith, preloadWarning, redirect, removeTrailingSlash, replaceEqualDeep, resolveManifestAssetLink, resolvePath, retainSearchParams, rootRouteId, scrollRestorationCache, setupScrollRestoration, storageKey, stringifySearchWith, stripSearchParams, trailingSlashOptions, trimPath, trimPathLeft, trimPathRight };
+export { BaseRootRoute, BaseRoute, BaseRouteApi, DEFAULT_PROTOCOL_ALLOWLIST, PathParamError, RawStream, RouterCore, SearchParamError, TSR_DEFERRED_PROMISE, buildDevStylesUrl, cleanPath, composeRewrites, createControlledPromise, createInlineCssStyleAsset, createNonReactiveMutableStore, createNonReactiveReadonlyStore, createRawStreamDeserializePlugin, createRawStreamRPCPlugin, createRouterConfig, createSerializationAdapter, decode, deepEqual, defaultGetScrollRestorationKey, defaultParseSearch, defaultSerializeError, defaultSerovalPlugins, defaultStringifySearch, defer, encode, escapeHtml, exactPathTest, executeRewriteInput, functionalUpdate, getAssetCrossOrigin, getElementScrollRestorationEntry, getInitialRouterState, getLocationChangeInfo, getMatchedRoutes, getStylesheetHref, getUnmaskOnReloadScript, handleHashScroll, interpolatePath, invariant, isDangerousProtocol, isInlinableStylesheet, isMatch, isModuleNotFoundError, isNotFound, isPlainArray, isPlainObject, isRedirect, isResolvedRedirect, joinPaths, lazyFn, makeSerovalPlugin, makeSsrSerovalPlugin, notFound, parseRedirect, parseSearchWith, preloadWarning, redirect, removeTrailingSlash, replaceEqualDeep, resolveManifestAssetLink, resolvePath, retainSearchParams, rootRouteId, routePathToRegExpSource, scrollRestorationCache, setupScrollRestoration, storageKey, stringifySearchWith, stripSearchParams, trailingSlashOptions, trimPath, trimPathLeft, trimPathRight };
diff --git a/dist/esm/path.js b/dist/esm/path.js
index 0f192cc..a000ad9 100644
--- a/dist/esm/path.js
+++ b/dist/esm/path.js
@@ -97,12 +97,50 @@ function resolvePath({ base, to, trailingSlash = "never", cache }) {
 	return result;
 }
 /**
+* Convert a route path template into a regular expression source that matches
+* concrete pathnames for that route.
+*/
+function routePathToRegExpSource(routePath) {
+	if (!routePath || routePath === "/") return "^/$";
+	let cursor = 0;
+	let regExpSource = "";
+	let segment;
+	while (cursor < routePath.length) {
+		const start = cursor;
+		segment = parseSegment(routePath, start, segment);
+		const end = segment[5];
+		cursor = end + 1;
+		if (start === end) continue;
+		const kind = segment[0];
+		if (kind === 0) {
+			regExpSource += `/${escapeRegExp(routePath.substring(start, end))}`;
+			continue;
+		}
+		const prefix = routePath.substring(start, segment[1]);
+		const suffix = routePath.substring(segment[4], end);
+		if (kind === 3) {
+			regExpSource += `(?:/${escapeRegExp(prefix)}[^/]+${escapeRegExp(suffix)})?`;
+			continue;
+		}
+		if (kind === 2) {
+			if (!prefix && !suffix) {
+				regExpSource += "(?:/.*)?";
+				continue;
+			}
+			regExpSource += `/${escapeRegExp(prefix)}.*${escapeRegExp(suffix)}`;
+			continue;
+		}
+		regExpSource += `/${escapeRegExp(prefix)}[^/]+${escapeRegExp(suffix)}`;
+	}
+	return `^${regExpSource}$`;
+}
+/**
 * Create a pre-compiled decode config from allowed characters.
 * This should be called once at router initialization.
 */
 function compileDecodeCharMap(pathParamsAllowedCharacters) {
 	const charMap = new Map(pathParamsAllowedCharacters.map((char) => [encodeURIComponent(char), char]));
-	const pattern = Array.from(charMap.keys()).map((key) => key.replace(/[.*+?^${}()|[\]\\]/g, "\\$&")).join("|");
+	const pattern = Array.from(charMap.keys()).map((key) => escapeRegExp(key)).join("|");
 	const regex = new RegExp(pattern, "g");
 	return (encoded) => encoded.replace(regex, (match) => charMap.get(match) ?? match);
 }
@@ -237,7 +275,10 @@ function encodePathParam(value, decoder) {
 	const encoded = encodeURIComponent(value);
 	return decoder?.(encoded) ?? encoded;
 }
+function escapeRegExp(value) {
+	return value.replace(/[.*+?^${}()|[\]\\]/g, "\$&");
+}
 //#endregion
-export { cleanPath, compileDecodeCharMap, exactPathTest, interpolatePath, joinPaths, removeTrailingSlash, resolvePath, trimPath, trimPathLeft, trimPathRight };
+export { cleanPath, compileDecodeCharMap, exactPathTest, interpolatePath, joinPaths, removeTrailingSlash, resolvePath, routePathToRegExpSource, trimPath, trimPathLeft, trimPathRight };
 
 //# sourceMappingURL=path.js.map
\ No newline at end of file
diff --git a/.tmp/router-core.new/dist/esm/unmask-on-reload-inline.js b/dist/esm/unmask-on-reload-inline.js
new file mode 100644
index 0000000..b11e761
--- /dev/null
+++ b/dist/esm/unmask-on-reload-inline.js
@@ -0,0 +1,4 @@
+//#region src/unmask-on-reload-inline.ts?script-string
+var unmask_on_reload_inline_default = "function(e){const o=e.routeMaskSources.map(a=>new RegExp(a)),t=window.history.state?.__tempLocation;t?.pathname&&(t.pathname===window.location.pathname&&(t.search??\"\")===window.location.search&&(t.hash??\"\")===window.location.hash||o.some(a=>a.test(t.pathname))&&window.location.replace(t.pathname+(t.search??\"\")+(t.hash??\"\")))}";
+//#endregion
+export { unmask_on_reload_inline_default as default };
diff --git a/.tmp/router-core.new/dist/esm/unmask-on-reload-script.js b/dist/esm/unmask-on-reload-script.js
new file mode 100644
index 0000000..fa522f2
--- /dev/null
+++ b/dist/esm/unmask-on-reload-script.js
@@ -0,0 +1,9 @@
+import { escapeHtml } from "./utils.js";
+import unmask_on_reload_inline_default from "./unmask-on-reload-inline.js";
+//#region src/unmask-on-reload-script.ts
+function getUnmaskOnReloadScript(routeMaskSources) {
+	if (!routeMaskSources.length) return null;
+	return `(${unmask_on_reload_inline_default})(${escapeHtml(JSON.stringify({ routeMaskSources }))})`;
+}
+//#endregion
+export { getUnmaskOnReloadScript };
